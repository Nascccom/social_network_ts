import {FC} from "react";

export const Header: FC<HeaderPropsType> = () => {
    return (
      <div>
          Header
      </div>
    );
};

// TYPES
type HeaderPropsType = {
    // section: string
}
